<%= button_to(
        "Log Out",
        destroy_user_session_path,
        method: :delete
      ) %>
<h1>New course</h1>
<%= render "form", course: @course %>

<br>

<div>
  <%= link_to "Back to courses", courses_path %>
</div>
